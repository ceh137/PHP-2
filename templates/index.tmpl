<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title> {{ title }} </title>
</head>

<body>

    <div class="header">
        <h1> {{ title }} </h1>
    </div>

    <div class="content">
        {% for photo in photos %}
        <div class="photo">
            <a href="showimage.php?img={{photo}}"><img class="BP" src="photos/{{ photo }}" alt="{{ photo }}"></a>
        </div>
        {% endfor %}
    </div>

    <div class="footer">
        <p>Copyright</p>
    </div>

</body>

</html>